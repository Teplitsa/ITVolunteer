import { ReactElement } from "react";

const TaskListFilter: React.FunctionComponent = (): ReactElement => {
  return <div className="task-list-skeleton__filter" />;
};

export default TaskListFilter;
