import { ReactElement } from "react";

const BeInTouchExplanation: React.FunctionComponent = (): ReactElement => {
  return <p>Смогу отвечать волонтёрам в течение суток.</p>;
};

export default BeInTouchExplanation;
